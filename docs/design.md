# Design

Option A: documentation only. Option B: platform shell plus validation core. Option C: full production host integration. Chosen: Option B for closed alpha because it gives runnable proof without over-scoping host-specific production work.
